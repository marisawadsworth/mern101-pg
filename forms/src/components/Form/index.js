import React, { Fragment } from 'react';

import { Button } from 'react-bootstrap'

//Create a function that posts to out api
//Using fetch

let PostToDB = (name) => {
    console.log("I pressed a button")
}